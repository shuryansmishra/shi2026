# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- LLM synthesis layer (optional -- template fallback works without it) ---
anthropic==0.34.2

# --- Geospatial / ingestion (optional at import time, but needed for real rasters) ---
rasterio==1.3.10
numpy==1.26.4
shapely==2.0.6
Pillow==10.4.0

# --- Image metrics ---
scikit-image==0.24.0

# --- ISRO data access ---
bhoonidhi

# --- Testing ---
pytest==8.3.3
httpx==0.27.2

# --- AI / ML (pinned to tested versions — do NOT upgrade without re-testing checkpoints) ---
# Tested combination: torch 2.8.0 / torchvision 0.23.0 / transformers 4.57.6
torch==2.8.0
torchvision==0.23.0
transformers==4.57.6
accelerate==1.8.1
bitsandbytes==0.45.5
qwen-vl-utils==0.0.10
requests==2.32.3

# --- Agentic Router (LangGraph StateGraph) ---
langgraph==0.3.34
langchain-core==0.3.65
