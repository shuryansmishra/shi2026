# Requirements for Render.com deployment (mock mode — no GPU/torch needed)
# Compatible with Render's free plan (512 MB RAM).
#
# For REAL ML inference on Render paid plan (2GB+ RAM):
#   1. Change plan to "standard" in render.yaml
#   2. Set VQA_MOCK_MODE=false in Render dashboard
#   3. Use requirements.txt instead of this file (has torch==2.8.0)
#
# torch, torchvision, rasterio are intentionally excluded —
# the backend handles their absence via HAS_TORCH / try-except guards.

# --- Core FastAPI stack ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Lightweight utilities ---
Pillow==10.4.0
numpy==1.26.4
requests==2.32.3
httpx==0.27.2
shapely==2.0.6

# --- LLM synthesis (template fallback works without ANTHROPIC_API_KEY) ---
anthropic==0.34.2

# --- Agentic Router ---
langgraph==0.3.34
langchain-core==0.3.65
